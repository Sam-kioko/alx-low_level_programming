#include "main.h"

/**
 * mul - Multiplies two intergers
 * @x: The first number to multiply
 * @y: The second number to multiply
 * Return: Result of multiplication between x and y
 */

int mul(int x, int y)
{
	return (x * y);
}
