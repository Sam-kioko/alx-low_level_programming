#include <stdio.h>

/**
 * main - Prints all the numbers of base sixteen in lowercase
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	for (i = 10; i <= 10; i++)
	{
		putchar (i);
	}
	for (i = 'a'; i <= 'f'; i++)
	{
		putchar (i);
	}
	putchar ('\n');

	return (0);
}
