#include <stdio.h>

/**
 * main - Prints all single digits of base ten from zero
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	return (0);
}
